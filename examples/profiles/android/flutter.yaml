# Flutter: builds the APK with flutter build apk --debug (the build container needs the Flutter SDK)
api_level: 30
build_tasks: assembleDebug
setup_commands: [flutter pub get, flutter build apk --debug]
test_runner: androidx.test.runner.AndroidJUnitRunner
emulator_params: -gpu swiftshader_indirect -no-audio -memory 3072
